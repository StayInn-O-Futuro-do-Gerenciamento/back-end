import { Request, Response } from "express";

export const createTypeRoomController = async (
  req: Request,
  res: Response
) => {};

export const listTypeRoomController = async (req: Request, res: Response) => {};

export const updateTypeRoomController = async (
  req: Request,
  res: Response
) => {};

export const deleteTypeRoomController = async (
  req: Request,
  res: Response
) => {};
